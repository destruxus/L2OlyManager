# Core Discord library (slash commands, threads, UI components).
discord.py>=2.3,<3.0

# Async SQLite driver.
aiosqlite>=0.19

# Timezone data (needed on some minimal Linux installs for ZoneInfo).
tzdata>=2024.1
